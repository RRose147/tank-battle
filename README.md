# 钢铁边境：坦克大战

一个使用原生 HTML、CSS 和 JavaScript Canvas 制作的坦克大战游戏。

## 运行

直接打开 `tank-battle/index.html`，或在项目根目录启动静态服务器：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000/tank-battle/`。

## 操作

- `WASD`：移动坦克
- `←` / `→`：旋转炮塔
- `空格`：发射炮弹
- 点击“重新开始”：重置战局
